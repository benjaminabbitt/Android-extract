[package]
name = "native-extractor"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
jni = "0.21"
libc = "0.2"
nix = { version = "0.27", features = ["process", "signal"] }

[profile.release]
opt-level = "z"
lto = true
strip = true
